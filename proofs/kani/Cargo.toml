[package]
name = "hibana-kani"
version = "0.0.0"
edition = "2024"
publish = false
autotests = false

[lib]
path = "../../src/lib.rs"

[workspace]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(hibana_repo_tests)', 'cfg(kani)'] }
